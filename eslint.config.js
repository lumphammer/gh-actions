import { defineConfig } from "eslint/config";
import coreConfig from "@lumphammer/shared-fvtt-bits/dotfiles/import/eslint.core.config.js";

const config = defineConfig([coreConfig]);

export default config;
